package logbuddy

import (
	"testing"
)

func TestServerManager(t *testing.T) {
	//create new server manager
	//start tcp server
	//start udp server
	//send messages to each server

	//stop each server
}
